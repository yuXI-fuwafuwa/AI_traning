## Day 1

感觉还行, 除了 j 期望 dp 花了点时间还没想出来, 最后看了题解解决了  

k 是 sg 函数, 按照递归定义写的, 很标准的博弈论, 学了 sg 函数和 nim 游戏之后写出来了  
i 是比较经典的二维 dp 吧, 不算难想, 应该没想多久  
l 是区间 dp, 区间 dp 的点和转移方向都很容易想到, 转移方程最后在确定了 dp 的定义之后也就呼之欲出了  
j 期望 dp, 以前没怎么做出来过, 都是概率 * 次数的老思维, 明显被爆了, 现在感觉会后向思考了  

dp 的定义和转移方程在注释里都有, 这里不重复写  

## Day 2

其实还是 d1 的时间做的, d1 做得太快了, 大概就早上一节半课就做完了  

cf 279 b 呃, 过于简单, 虽然第一想法是前缀和二分, 但是马上想到能用双指针线性处理, 没什么难度, 几分钟就出来了  

稍微偷看了后面的题目, 呃我感觉上一年网络赛的中档题和这些签到题都算不上的题根本不能算循序渐进地衔接  
呃怎么会出这些远古场的 ab 题, 是不是思路秒出, 代码 5 min 内解决  
待会和 GPT 提个意见, 让她找点呃, 更符合 2026 年强度的题目?  

cf 1485 a 很简单, 由于一直 /2 的次数是 log a 级别的, 而 b = 1 的情况不用 2 操作不可能, 其他情况只会少于 log a 次除法  
因此遍历所有比除 max(原始 b, 2) 可能更优的 bb 即可统计答案, 复杂度大概 O(Tlog^2A)

cf 1542 b, 和上一题差不多, 一样是 /a 保证复杂度, 推导在注释  

cf 1701 c, 二分答案  

cf 1615 b, 非常简单, 由于需要 AND 不是 0, 必然有一位全是 1, 则找出最少的 0 的那位就行了  
由于 l r 有固定范围, 预处理每一位的 1 数量前缀和, 则 长度 - 区间1数量 那就是需要移除的数字  
取最小值即可  

每道题都可以在 5 min 内写完, 不知道何意味  

## Day 3

广义最短路  

对于一个有向图 G = {V, E}, v in V 有函数 f(v); e in E 有函数 g_e(f(v));  
定义:  
f(v) = min{ g_e(f(p)) | e = p->v };  
g_e(f(v)) >= f(v);  
x >= y, g_e(x) >= g_e(y);  
f(s) = init_s, s 为源点;  

求所有 f(v), 可以使用 dijkstra 算法求解, 证明待会看一下算法导论然后抄一下了, 感觉比 AI 讲得好  
(才发现跳了一天)  

**初始化：** 初始化时，\(S=\emptyset\)，因此，循环不变式直接成立。

**保持：** 我们希望证明在每次循环中，对于加入到集合 \(S\) 的结点 \(u\) 来说，\(u.d=\delta(s, u)\)。我们使用反证法来证明此论断。设结点 \(u\) 是第一个在加入到集合 \(S\) 时使得该方程式不成立的结点，即 \(u.d \neq \delta(s, u)\)。我们下面将注意力集中到把结点 \(u\) 加入到集合 \(S\) 的这遍循环的开始，并通过对从结点 \(s\) 到结点 \(u\) 的最短路径进行检查来导出结论 \(u.d=\delta(s, u)\)。由于结点 \(s\) 是第一个加入到集合 \(S\) 中的结点，并且 \(s.d=\delta(s, s)=0\)，结点 \(u\) 必定与结点 \(s\) 不同，即 \(u \neq s\)。因为 \(u \neq s\)，在即将把结点 \(u\) 加入到集合 \(S\) 时，我们有 \(S \neq \emptyset\)。此时，一定存在某条从结点 \(s\) 到结点 \(u\) 的路径，否则，根据非路径性质将有 \(u.d=\delta(s, u)=\infty\)，而这将违反我们的假设 \(u.d \neq \delta(s, u)\)。因为至少存在一条从 \(s\) 到 \(u\) 的路径，所以也存在一条从 \(s\) 到 \(u\) 的最短路径 \(p\)。在将结点 \(u\) 加入到集合 \(S\) 之前，路径 \(p\) 连接的是集合 \(S\) 中的一个结点（即 \(s\)）和 \(V-S\) 中的一个结点（即 \(u\)）。让我们考虑路径 \(p\) 上第一个满足 \(y \in V-S\) 的结点 \(y\)，设 \(x \in S\) 为结点 \(y\) 在路径 \(p\) 上的前驱，如图 24-7 所示，我们可以将路径 \(p\) 分解为 \(s \stackrel{p_1}{\rightsquigarrow} x \rightarrow y \stackrel{p_2}{\rightsquigarrow} u\)。（路径 \(p_1\) 或者 \(p_2\) 可能不包含任何边。）

我们断言：在将结点 \(u\) 加入到集合 \(S\) 时，\(y.d=\delta(s, y)\)。为了证明这一点，只要观察到 \(x \in S\)。然后，因为选择的结点 \(u\) 是第一个在加入到集合 \(S\) 时不满足条件 \(u.d \neq \delta(s, u)\) 的结点，在将结点 \(x\) 加入到集合 \(S\) 时，有 \(x.d=\delta(s, x)\)。此时，边 \((x, y)\) 将被松弛，根据收敛性质可以得出我们的断言。

现在可以通过反证来证明 \(u.d=\delta(s, u)\)。因为结点 \(y\) 是从结点 \(s\) 到结点 \(u\) 的一条最短路径上位于 \(u\) 前面的一个结点，并且所有的边权重均为非负值，所以有 \(\delta(s, y) \leqslant \delta(s, u)\)，因此，

$$
\begin{aligned}
y.d &= \delta(s, y) \\
&\leqslant \delta(s, u) \\
&\leqslant u.d \quad (\text{根据上界性质}) \qquad (24.2)
\end{aligned}
$$

但是，因为在算法第5行选择结点 \(u\) 时，结点 \(u\) 和 \(y\) 都在集合 \(V-S\) 里，所以有 \(u.d \leqslant y.d\)。因此，式(24.2)中的两个不等式事实上都是等式，即

$$
y.d = \delta(s, y) = \delta(s, u) = u.d
$$

因此 \(u.d=\delta(s, u)\)，而这与我们选择的结点 \(u\) 矛盾。因此，我们推断，在结点 \(u\) 被加入到集合 \(S\) 时有 \(u.d=\delta(s, u)\)，并且该等式在随后的所有时间内都保持成立。

**终止：** 在算法终止时，\(Q=\emptyset\)。该事实与之前的不变式 \(Q=V-S\) 一起说明了 \(S=V\)。因此，对于所有的结点 \(u \in V\)，有 \(u.d=\delta(s, u)\)。 \(\blacksquare\)

1. 分层图最短路, 0 层表示没有用优惠券, 1 层表示用了, 01 之间的边为半价, 层内边原价  
2. 广义最短路



## Day 4

写数据结构真是如鱼得水  

1. 二分图染色
2. 异或哈希(da), 或者也可以用(db)的乱搞, 核心是质数指数的奇偶性观察
3. 正解应该是 std::set 乱搞, 因为 k 只有 10; 写了线段树合并, 感觉很好  
4. 