# 对所有文本文件自动检测，并强制在检出时使用 LF
* text=auto eol=lf

# 可选：为特定类型文件强制使用 LF
*.cc text eol=lf
*.md text eol=lf

# 例外：Windows 批处理脚本通常需要 CRLF 才能正常运行
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何转换
*.png binary
*.jpg binary